# Screenshot Checklist

Save captured PNGs to **`docs/judging/screenshots/`** using these exact filenames (user adds images before final submission):

| File | Screen |
| --- | --- |
| `01-home.png` | Home dashboard (`/(editor)/index`) with active project card |
| `02-import.png` | Import flow (`/(editor)/import`) before and after media selection |
| `03-photo.png` | Photo AI screen (`/(editor)/photo`) with retouch + background template selected |
| `04-video.png` | Video edit screen (`/(editor)/video`) showing trim window + active filters |
| `05-export.png` | Export lab (`/(editor)/export`) with preset list and generated outputs |
| `06-history.png` | History gallery (`/(editor)/history`) with at least two projects and re-open action |

> **Status:** folder and naming convention are ready; PNG files are **not checked in yet** — capture on device (dark mode, no debug overlays) and drop into `docs/judging/screenshots/`.

Capture guidance (same device frame throughout):

1. Home dashboard (`/(editor)/index`) with active project card
2. Import flow (`/(editor)/import`) before and after media selection
3. Photo AI screen (`/(editor)/photo`) with retouch + background template selected
4. Video edit screen (`/(editor)/video`) showing trim window + active filters
5. Export lab (`/(editor)/export`) with preset list and generated outputs
6. History gallery (`/(editor)/history`) with at least two projects and re-open action

Quality guardrails:
- Keep text readable at thumbnail size
- Show one clear “hero action” per screenshot
- Maintain consistent top spacing and tab bar visibility
